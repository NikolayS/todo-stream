# todo-stream

A tiny CLI that streams TODO/FIXME/HACK comments out of a codebase with git
blame context (author + commit date) and emits them as JSON or human-readable
markdown.

Built live with [samospec](https://github.com/NikolayS/samospec) v0.3.0.
